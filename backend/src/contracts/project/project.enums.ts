export enum ProjectColumnKey {
  backlog = 'backlog',
  in_progress = 'in_progress',
  in_review = 'in_review',
  done = 'done',
}

export enum ProjectStatus {
  ACTIVE = 'ACTIVE',
  ARCHIVED = 'ARCHIVED',
}
